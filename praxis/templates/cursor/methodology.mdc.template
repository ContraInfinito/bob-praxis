---
description: Praxis-generated project context and methodology principles for {project_name}
globs:
alwaysApply: true
---

# {project_name} — project context and methodology

{granite_agents_context}

**Stack:** {stack_name}
**Frameworks:** {frameworks_list}
**Dependencies:** {dependencies_list}
{clarifying_questions_block}
## Methodology principles

When working on this codebase, follow these. They are project conventions, not suggestions. They were generated by Praxis from the developer's chosen methodology.

{methodology_principles_short}

## What to avoid

- Never add credentials, API keys, or tokens to any file. Use environment variables.
- Never modify generated files or anything under `.git/`, `venv/`, `__pycache__/`, or `node_modules/`.
- Never introduce dependencies without justifying the addition and confirming it fits the project's scope.
- When in doubt about a design decision, ask before implementing — see "Prompt-first execution" above.

---

*Generated by Praxis on {generation_date}. See https://github.com/ContraInfinito/bob-praxis*

# Project Context for Bob

**Project**: {project_name}  
**Stack**: {stack_name}  
**Frameworks**: {frameworks_list}  
**Generated**: {generation_date}

## What This Project Is

{granite_agents_context}

## Trigger Keywords

When the user's message contains the keyword `design` or `scope` (case-insensitive, anywhere in the message), enter STRICT prompt-first mode. Produce a six-field structured prompt and wait for explicit user approval before any action — even if the request appears clear.

The six fields, in order:

1. **Restated request** — bullet-point reformulation of what you think the user wants
2. **Assumptions** — every assumption you would make if you proceeded as-is
3. **Scope IN** — what is included in the work
4. **Scope OUT** — what is deliberately excluded
5. **Proposed approach** — one or two sentences summarizing your implementation strategy
6. **Open questions** — anything you need clarified before starting

Wait for the developer to approve, edit, or reject the structured prompt. Do not write code, edit files, or run commands until they approve.

Example trigger phrases: "Design a rate limiter", "Scope out the auth system", "Help me design the database schema". Plain questions like "what does this file do?" or "run the tests" do not trigger strict mode.

## How to Work on This Project

This project uses a structured AI-collaboration methodology defined in `PRAXIS_CONTRACT.md`. Before making changes, read that contract to understand how Bob should work with you on this specific project.

### Key Resources

- **PRAXIS_CONTRACT.md** — The collaboration contract defining how Bob works with you
- **python_skill.md** — Python-specific conventions and framework guidance for this project
- **methodology_skill.md** — The 7 methodology principles Bob follows
- **.bobignore** — Files Bob should never read or modify

### Detected Stack Details

- **Python files**: {python_files_count}
- **Dependencies**: {dependencies_list}

## Session Start Checklist

When starting a new session on this project:

1. Read PRAXIS_CONTRACT.md to refresh on the collaboration contract
2. Check python_skill.md for stack-specific conventions
3. Review .bobignore to know what files are off-limits
4. Ask clarifying questions before acting on ambiguous requests
{clarifying_questions_block}
## Methodology Principles (Quick Reference)

{methodology_principles_short}

For full details on each principle, see PRAXIS_CONTRACT.md.